This directory holds project-wide styles.

- Prefer Tailwind CSS utilities in components and pages.
- If you need custom CSS, create modular files here and import them where needed.
- Keep global resets in app/app.css (already present).
