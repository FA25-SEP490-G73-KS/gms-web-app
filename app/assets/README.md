This directory stores static assets (images, icons, docs) scoped to the application.

- Reuse existing welcome logos for now to avoid breaking the homepage header.
- Add new assets here and import them with Vite's asset handling.
